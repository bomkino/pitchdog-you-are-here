{
  "name": "@pitch-dog/you-are-here",
  "version": "1.1.0",
  "description": "A deterministic next-move compass for pitches from pitch.dog.",
  "license": "AGPL-3.0-or-later",
  "homepage": "https://pitchdog-you-are-here.dog-pitch.chatgpt.site",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bomkino/pitchdog-you-are-here.git"
  },
  "bugs": {
    "url": "https://github.com/bomkino/pitchdog-you-are-here/issues"
  },
  "keywords": [
    "pitching",
    "decision-tree",
    "creative-work",
    "local-first",
    "accessibility"
  ],
  "engines": {
    "node": ">=22.18"
  },
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check": "tsc --noEmit",
    "test": "node --test tests/*.test.ts",
    "build": "vite build && node scripts/build-sites-worker.mjs",
    "test:hosting": "node --test tests/hosting.test.mjs",
    "verify": "npm run check && npm run test && npm run build && npm run test:hosting"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3",
    "vite": "^7.0.0"
  }
}
