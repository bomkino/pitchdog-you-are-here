import assert from "node:assert/strict"; import { readFile } from "node:fs/promises"; import { extname, resolve } from "node:path"; import test from "node:test"; import { fileURLToPath } from "node:url";
const workerURL = new URL("../dist/server/index.js", import.meta.url); workerURL.searchParams.set("test", `${process.pid}-${Date.now()}`); const { default: worker } = await import(workerURL.href); const clientRoot = fileURLToPath(new URL("../dist/client/", import.meta.url)); const types = { ".css":"text/css", ".html":"text/html", ".js":"text/javascript", ".png":"image/png", ".svg":"image/svg+xml", ".webp":"image/webp" }; const env={ASSETS:{async fetch(request){const url=new URL(request.url);const relative=url.pathname==="/"?"index.html":url.pathname.slice(1);try{const body=await readFile(resolve(clientRoot,relative));return new Response(request.method==="HEAD"?null:body,{status:200,headers:{"content-type":types[extname(relative)]??"application/octet-stream"}})}catch{return new Response("Not found",{status:404})}}}}; const ctx={waitUntil(){},passThroughOnException(){}};
test("serves You Are Here with security headers",async()=>{const response=await worker.fetch(new Request("https://example.test/",{headers:{accept:"text/html"}}),env,ctx);assert.equal(response.status,200);assert.match(await response.text(),/You Are Here/);assert.equal(response.headers.get("x-content-type-options"),"nosniff")}); test("deep links fall back",async()=>{const response=await worker.fetch(new Request("https://example.test/route",{headers:{accept:"text/html"}}),env,ctx);assert.equal(response.status,200)});
